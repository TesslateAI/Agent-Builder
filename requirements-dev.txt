# Development requirements for Agent-Builder
# Install TFrameX separately from local folder:
# pip install -e ../TFrameX

# Core dependencies
flask>=3.1.0
flask-cors>=5.0.0
python-dotenv>=1.0.0
httpx>=0.25.0
pydantic>=2.0.0
PyYAML>=6.0.1
aiohttp>=3.9.0

# TFrameX dependency (matches production)
tframex==1.1.0

# Development tools
pytest>=7.0.0
black>=22.0.0
ruff>=0.1.0
mypy>=1.0.0